<a href='/'>
  <div class='fig'>
    <img src={{post_image}} alt='{{post_title}}' style='objectFit:cover' />
    <div class='caption'>
      <h3>
        {{#if likes}}
          <i class='fa fa-heart'></i><span> {{likes.length}}</span>
        {{/if}}
      </h3>
      <h5>Sign-up or login to view content!</h5>
    </div>
  </div>
</a>